apiVersion: 1

providers:
  - name: URL Shortener Live
    orgId: 1
    folder: URL Shortener
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
